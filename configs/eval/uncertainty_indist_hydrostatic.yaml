defaults:
  - ../model/fno.yaml

eval:
  dataset_path: data/processed/hydrostatic/test
  batch_size: 64
  output_dir: experiments/fno/eval_uncertainty_indist

uncertainty:
  ensemble_checkpoints: []
  min_ensemble_members: 2
  interval_levels: [0.5, 0.8, 0.9, 0.95]
  report_physical_metrics: true
