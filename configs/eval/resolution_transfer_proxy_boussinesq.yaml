defaults:
  - ../model/fno_boussinesq.yaml

eval:
  dataset_path: data/processed/boussinesq/test
  output_dir: experiments/fno_boussinesq/eval_resolution_proxy
  report_physical_metrics: true

resolution_transfer:
  eval_resolutions: [32, 64, 128]
