base_config: fno.yaml

project:
  name: tsunami-fno-physics-loss

paths:
  output_root: results/fno_physics_run
  checkpoint_dir: results/fno_physics_run/checkpoints
  log_dir: results/fno_physics_run/logs
  figure_dir: results/fno_physics_run/figures

loss:
  mse_weight: 1.0
  l1_weight: 0.05
  gradient_weight: 0.10
  temporal_weight: 0.08
  mass_weight: 0.08
  spectral_weight: 0.03
