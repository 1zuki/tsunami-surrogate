defaults:
  - ../train/base.yaml

output_dir: experiments/unet

model:
  name: unet
  in_channels: 3
  out_channels: 80
  width: 32

eval:
  dataset_path: data/processed/hydrostatic/test
  batch_size: 8
  output_dir: experiments/unet/eval
