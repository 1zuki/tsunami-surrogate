defaults:
  - fno.yaml

# Dedicated uncertainty protocol. Ordinary FNO training deliberately does not
# inherit member seeds; use scripts/train_ensemble.py with this config.
ensemble:
  seeds: [11, 22, 33, 44, 55, 66, 77]
  member_dir_template: experiments/ensemble/member_{seed}
