defaults:
  - ../fno.yaml

# Seed 18 is already complete at experiments/fno/seed_18/best.pt. These two fresh
# runs complete the frozen three-seed architecture/reference matrix.
seeds: [36, 67]
output_dir: experiments/fno

eval:
  output_dir: experiments/fno/eval
