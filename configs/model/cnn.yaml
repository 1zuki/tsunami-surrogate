defaults:
  - ../train/base.yaml

output_dir: experiments/cnn

model:
  name: cnn
  in_channels: 3
  out_channels: 50
  width: 48

eval:
  dataset_path: data/processed/hydrostatic/test
  batch_size: 8
  output_dir: experiments/cnn/eval
