base_config: fno.yaml

project:
  name: curriculum-stage-64

paths:
  output_root: results/curriculum_64
  checkpoint_dir: results/curriculum_64/checkpoints
  log_dir: results/curriculum_64/logs
  figure_dir: results/curriculum_64/figures
  dataset_dir: data/synthetic/curriculum_64
  train_file: data/synthetic/curriculum_64/train.npz
  val_file: data/synthetic/curriculum_64/val.npz
  test_file: data/synthetic/curriculum_64/test.npz
  stats_file: data/synthetic/curriculum_64/stats.yaml

simulation:
  nx: 64
  ny: 64
  nt: 20

model:
  out_channels: 20
  hidden_channels: 16
  modes_x: 12
  modes_y: 12
  n_layers: 3

training:
  epochs: 25
  batch_size: 2
  lr: 0.0007

dataset_generation:
  n_samples: 384
