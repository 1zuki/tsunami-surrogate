\section{Surrogate models}
\label{sec:models}

The feedforward direct models map three static input fields to all 50 target frames in a single pass using the interface in Section~\ref{subsec:forward-task}. Conversely, ConvLSTM processes the same static inputs to emit the trajectory recurrently, initializing with zero feedback and feeding autoregressive predictions into subsequent steps. The seeded window models formulate a distinct task, taking four input channels (two static, two dynamic elevation) to predict five output frames per block. Despite sharing a final output shape, these explicitly distinct interfaces reflect non-equivalent internal prediction dynamics. Here, the Fourier Neural Operator serves as the primary direct benchmark, with remaining models offering spectral, convolutional, encoder--decoder, and recurrent baselines.

The canonical sources are cited in the direct-comparison table and at first use below. Each implementation adapts the cited architecture to the shared three-input, 50-output task and is not intended as an exact reproduction of the original training setup.

\subsection{Fourier Neural Operator}
\label{subsec:fno}

The Fourier Neural Operator (FNO) is our primary model because it learns a mapping between discretized functions rather than a fixed pixel-to-pixel function, and its spectral layer gives it a global receptive field at every depth \citep{li2021fno}. This suits tsunami-like propagation, where a disturbance influences distant parts of the domain within the forecast horizon and a purely local model would need many layers to reach the same range.

Figure~\ref{fig:fno-arch} illustrates the overall architecture. The input, concatenated with two normalized coordinate channels for spatial awareness, is first lifted to a width-$d$ feature map via a $1 \times 1$ convolution. The features are then processed by a stack of $L$ Fourier blocks combining a spectral convolution $\mathcal{K}$ and a local path $W$ with a GELU activation:
\begin{equation}
z \mapsto \mathrm{GELU}\!\left(\mathcal{K}(z) + W z\right).
\end{equation}
In the spectral path, features are transformed via a 2D real Fast Fourier Transform (FFT), truncated to the lowest $m_1 \times m_2$ modes, multiplied by learnable complex weight tensors, and inverted back. Mode truncation acts as a regularizer and permits the same learned mode weights to be applied on different grid sizes, enabling cross-resolution evaluation without retraining; it does not imply resolution independence. Boundary reflection artifacts from FFT periodicity are mitigated by zero-padding prior to the blocks and cropping afterward. A two-layer pointwise head then projects features to $T$ output frames.

The baseline configuration uses $L = 6$ blocks, width $d = 48$, modes $m_1 = m_2 = 14$, and 6-cell padding, trained independently across hydrostatic, MUSCL-HR, and Boussinesq labels. Two hydrostatic ablations (FNO-modes8 and FNO-modes20) evaluate mode counts of 8 and 20 per axis while keeping all other hyperparameters fixed.

\begin{figure}[pos=htbp]
\centering
\begin{tikzpicture}[
    font=\small,
    box/.style={draw, rounded corners=2pt, minimum height=8mm, minimum width=11mm, align=center, inner sep=3pt},
    lift/.style={box, fill=blue!10},
    block/.style={box, fill=orange!15},
    head/.style={box, fill=green!12},
    io/.style={box, fill=gray!10},
    op/.style={draw, circle, inner sep=1pt, minimum size=6mm, fill=orange!20},
    sm/.style={box, fill=orange!8, minimum height=7mm, minimum width=9mm},
    >={Stealth[length=2mm]},
    arr/.style={->, thick},
]

\node[io] (in) {$X$\\\scriptsize $3{\times}H{\times}W$};
\node[lift, right=7mm of in] (lift) {Lift\\\scriptsize $1{\times}1$ conv\\\scriptsize $\to d{=}48$};
\node[block, right=7mm of lift] (b1) {Fourier\\block $1$};
\node[block, right=5mm of b1] (b2) {Fourier\\block $2$};
\node[right=4mm of b2] (dots) {$\cdots$};
\node[block, right=4mm of dots] (bL) {Fourier\\block $6$};
\node[head, right=6mm of bL] (proj) {Proj.\\\scriptsize head};
\node[io, right=6mm of proj] (out) {$\widehat{Y}$\\\scriptsize $50{\times}H{\times}W$};

\draw[arr] (in) -- (lift);
\draw[arr] (lift) -- (b1);
\draw[arr] (b1) -- (b2);
\draw[arr] (b2) -- (dots);
\draw[arr] (dots) -- (bL);
\draw[arr] (bL) -- (proj);
\draw[arr] (proj) -- (out);

\node[align=center, below=2mm of lift, text width=24mm, font=\scriptsize] {+\,2 grid coord\\channels.};

\node[draw, dashed, rounded corners, fit={(b1) (bL)}, inner sep=4pt] (fitblk) {};
\coordinate (dl) at ($(fitblk.south)+(0,-3mm)$);

\node[sm] (vin) at ($(dl)+(-4.7,-1.6)$) {$z$};

\node[op, right=6mm of vin] (fft) {$\mathcal{F}$};
\node[sm, right=5mm of fft] (modes) {keep\\\scriptsize $14{\times}14$\\\scriptsize modes};
\node[op, right=5mm of modes] (rmul) {$R$};
\node[op, right=5mm of rmul] (ifft) {$\mathcal{F}^{-1}$};
\node[draw, circle, inner sep=0pt, minimum size=5mm, right=5mm of ifft, fill=red!15] (sum) {$+$};
\node[op, right=5mm of sum, fill=red!20] (act) {$\sigma$};
\node[sm, right=5mm of act] (vout) {$z'$};

\node[op, below=7mm of rmul, fill=blue!15] (W) {$W$};

\draw[arr] (vin) -- (fft);
\draw[arr] (fft) -- (modes);
\draw[arr] (modes) -- (rmul);
\draw[arr] (rmul) -- (ifft);
\draw[arr] (ifft) -- (sum);
\draw[arr] (sum) -- (act);
\draw[arr] (act) -- (vout);
\draw[arr] (vin) |- (W);
\draw[arr] (W) -| (sum);

\draw[densely dotted] (fitblk.south) -- ($(fft)!0.5!(rmul)+(0,0.95)$);


\node[align=left, font=\scriptsize, right=2mm of vout, text width=20mm]
  {$\sigma=\mathrm{GELU}$\\spectral $+$ local};
\end{tikzpicture}
\caption{FNO architecture used in this work. \textbf{Top:} the input field $X$ (bathymetry, raw source template, initial depth; $C{=}3$) plus two grid-coordinate channels is lifted to width $d{=}48$, passed through $L{=}6$ Fourier blocks, and projected to the $T{=}50$ forecast frames. \textbf{Bottom:} one Fourier block. The spectral path applies a real FFT, keeps the lowest $14{\times}14$ modes, multiplies them by a learnable complex weight $R$, and inverts the transform; the local path $W$ is a $1{\times}1$ convolution. Their sum passes through a GELU. Truncating to the low modes is what lets the same weights run at a different grid resolution.}
\label{fig:fno-arch}
\end{figure}

\subsection{Operator variants}
\label{subsec:operator-variants}
Three additional neural-operator variants extend the single-pass baseline. The Factorized Fourier Neural Operator (\textbf{F-FNO}) replaces dense 2D spectral mixing with separable 1D Fourier transforms along each spatial axis \citep{tran2023ffno}:
\begin{equation}
\mathcal{K}_{\mathrm{F\text{-}FNO}}(z) = \mathcal{F}_x^{-1}\!\left(R_x \mathcal{F}_x z\right) + \mathcal{F}_y^{-1}\!\left(R_y \mathcal{F}_y z\right).
\end{equation}

This structural factorization reduces the parameter complexity per block from $\mathcal{O}(d^2 m_1 m_2)$ to $\mathcal{O}(d^2 (m_1 + m_2))$, accounting for the parameter reduction in Table~\ref{tab:accuracy}. Additionally, we evaluate the U-shaped FNO (\textbf{U-FNO}), which incorporates multiscale U-Net skip connections \citep{wen2022ufno}, and the Wavelet Neural Operator (\textbf{WNO}), which replaces Fourier bases with multiresolution wavelets \citep{tripura2023wno}. All variants share identical input features, loss functions, and evaluation metrics to isolate architectural performance.

\subsection{Windowed rollout variants}
\label{subsec:window-models}
To assess long-horizon autoregressive drift, we train two seeded window models—Window-FNO-5 and Window-F-FNO-5—on the hydrostatic reference data. Let $Y_j$, $j = 0,\ldots,49$, denote the 50 target frames. A training window starting at frame index $s$ maps input $X_s^{\mathrm{win}} = [b, q, Y_s, Y_{s-1}]$ to target block $Y_s^{\mathrm{win}} = [Y_{s+1}, \dots, Y_{s+5}]$, setting $Y_{-1} = Y_0$ for the initial step:
\begin{equation}
X_s^{\mathrm{win}} \mapsto Y_s^{\mathrm{win}} = [Y_{s+1}, \dots, Y_{s+5}].
\end{equation}
Window starts $s \in \{0, 5, \dots, 40, 44\}$ yield ten five-frame targets per 50-frame trajectory; the clamped final window overlaps the preceding target block by one frame to remain in range.

During inference, given the initial observation $Y_0$ as a rollout seed, the model predicts sequential five-frame blocks autoregressively by feeding its final two predictions into subsequent iterations. Evaluation metrics cover predicted frames $Y_1,\ldots,Y_{49}$ and exclude the seed. These windowed configurations measure conditional error accumulation and are not like-for-like direct forecasts.

\subsection{Convolutional and recurrent baselines}
\label{subsec:conv-baseline}
To check whether the spectral, global structure of the FNO actually helps, we include local convolutional, encoder-decoder, and recurrent baselines.

The \textbf{CNN} is a small fully convolutional neural network: $3 \times 3$ convolutions followed by a GELU activation, then a $1 \times 1$ output convolution that produces $T$ output frames at a width-$d = 48$. It has no down-sampling and no global mixing, so its receptive field grows only with depth. It therefore represents the simplest reasonable local predictor and is useful as a lower comparison anchor.

The \textbf{U-Net} incorporates a single-level encoder-decoder with bilinear upsampling and skip connections at a base width of 32 \citep{ronneberger2015unet}. Spatial average pooling expands its effective receptive field, offering a stronger multiscale convolutional baseline.

The \textbf{ConvLSTM} baseline evaluates explicit temporal recurrence \citep{shi2015convlstm}. A two-layer encoder maps the three static fields $[b,q,h_0]$ to a static context and the initial hidden and cell states. At step $t$, $r_t$ combines this context with a $1 \times 1$ projection of the previous predicted frame; feedback is zero initially, and no true target frame is supplied. Two recurrent layers ($48$ hidden channels, $3 \times 3$ kernels) update states via spatial convolutions ($*$):
\begin{equation}
\begin{bmatrix} i_t \\ f_t \\ o_t \\ g_t \end{bmatrix} = \begin{bmatrix} \sigma \\ \sigma \\ \sigma \\ \tanh \end{bmatrix} \!\left(W_\mathrm{gate} * [r_t, h_{t - 1}]\right),
\end{equation}
\begin{equation}
c_t = f_t \odot c_{t - 1} + i_t \odot g_t, \qquad h_t = o_t \odot \tanh(c_t).
\end{equation}
Single-frame surface predictions are produced sequentially via a $1 \times 1$ readout and fed autoregressively into the next step, initializing with zero feedback. This baseline tests whether recurrent feedback improves long-horizon trajectory fidelity despite higher computational cost.

\subsection{Ensemble uncertainty}
\label{subsec:model-ensemble}
Uncertainty estimation is implemented via a deep ensemble \citep{lakshminarayanan2017deepensembles} of $M = 7$ independently seeded FNO models without structural modifications. Point predictions are defined by the ensemble mean, while the per-element predictive spread serves as the variance metric (Section~\ref{subsec:uncertainty}). Because members share deterministic architectures and training data, this variance primarily reflects seed-dependent optimization variability rather than aleatoric observation noise.
