\section{Results}
\label{sec:results}

\subsection{Reference-solver validation and dataset quality}
% TODO: Report final manifest counts, quality_status summary, and basic solver validation outcomes.

\subsection{Same-solver surrogate accuracy}
% TODO: Table: FNO on hydrostatic, MUSCL-HR, and Boussinesq; CNN/U-Net/ConvLSTM on the shallow-water targets unless the extra baseline runs are completed.
% TODO: Figure: qualitative rollout example for the Hydrostatic target: bathymetry, source, reference eta, FNO eta, and error at early/mid/late saved timesteps. If space allows, add MUSCL-HR and Boussinesq comparison rows using the same scenario ID.
% TODO: Optional visual figure: 3D surface-view snapshot of eta over bathymetry at one selected timestep, mainly for intuition. Keep the quantitative comparison in the 2D panels/tables so the 3D plot does not overclaim accuracy.

\subsection{Runtime and speedup}
% TODO: Table: CPU solver rollout time, CPU/GPU model inference time, precision, batch size, and speedup.
% DATASET NOTE: train 10k (seed 42); val 1k (separate fixed seed); test 2.5k (separate fixed seed). plus native 1k @32x32 and 1k @128x128 for cross-resolution. generation wall-clock ~2.5d for the 10k train pool.

\subsection{Distribution-shift generalization}
% TODO: Report source-family, bathymetry-family, and source-strength OOD suites; include sample counts.

\subsection{Cross-resolution transfer}
% TODO: Report proxy and/or native-resolution results; explicitly state normalization policy.

\subsection{Synthetic-to-real bathymetry transfer}
% TODO: Train on 10k synthetic 64x64; test on real bathymetry patches with synthetic sources and a hydrostatic reference. Report MAE / rel-L2 on synthetic-test vs real-bathymetry side by side, the degradation factor, qualitative wavefield comparison, and whether ensemble uncertainty rises on the real patches.

\subsection{Solver-fidelity and cross-solver diagnostics}
% TODO: Report all shared-scenario raw solver gaps; keep Hydrostatic as the primary reference and present MUSCL-HR plus Boussinesq as the two comparison baselines.

\subsection{Uncertainty}
% TODO: Report ensemble calibration/error-uncertainty correlation and OOD uncertainty behavior.

\subsection{Boussinesq comparison reference}
% TODO: Show Boussinesq same-solver result and pairwise raw gaps as the weakly dispersive comparision baseline.
